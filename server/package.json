{
  "name": "express",
  "version": "1.0.0",
  "description": "定义语法，写javascript和node都必须遵守\r 变量定义,循环、判断、函数\r 原型和原型链、作用域和闭包、异步",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "cross-env NODE_ENV=dev nodemon ./bin/www.js",
    "pro": "cross-env NODE_ENV=production nodemon ./bin/www.js"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "cross-env": "^7.0.3",
    "mysql": "^2.18.1",
    "nodemon": "^2.0.7",
    "redis": "^3.1.2"
  }
}
